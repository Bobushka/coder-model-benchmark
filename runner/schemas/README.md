# Runner Schemas

Current structured artifacts:

- `run.json` — one benchmark run result
- `tech-profile.json` — one technical capability snapshot

These are emitted by `runner/schemas/run_schema.py`.
