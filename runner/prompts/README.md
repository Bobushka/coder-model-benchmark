# Shared Prompt Wrappers

This folder will hold shared wrapper text for native harness runs.

The wrapper layer may add:

- path to the canonical `AGENTS.md`;
- path to `TASK.md`;
- submission reminder;
- review artifact reminder.

It must not rewrite the task itself.
