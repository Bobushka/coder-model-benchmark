# Results

This folder stores benchmark evidence.

- `runs/` — raw run directories.
- `reports/` — human-readable summaries.

Raw evidence should remain preserved even when final summaries are rewritten.

