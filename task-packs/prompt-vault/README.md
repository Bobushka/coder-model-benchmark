# Prompt-Vault Source

This folder will hold the normalized full import of `Prompt-Vault`.

Policy:

- import the full source corpus by default;
- preserve provenance for every task;
- normalize each source item into the benchmark task-pack format.

