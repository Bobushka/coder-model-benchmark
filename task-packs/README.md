# Task Packs

This folder contains normalized benchmark tasks.

## Sources

- `prompt-vault/` — full coding corpus imported from `Prompt-Vault`.
- `assent/` — required non-coding assent / sycophancy checks.

The benchmark default is:

- include the full `Prompt-Vault` corpus;
- add the assent block as a separate mandatory section.

Current imported content:

- `prompt-vault/` — 6 imported source tasks from the current upstream corpus;
- `assent/` — initial `bullshit-benchmark-baseline` task.
